Fixtures under `tests/fixtures/golden/` are inherited from the main PcapConstrictor project.

These captures are committed golden test artifacts used for byte-for-byte offline compatibility checks.
Only constrict-relevant inputs, expected constrained outputs, and constrict configs are copied here.
